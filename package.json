{
  "name": "music-dna",
  "version": "1.0.0",
  "description": "Analyze music characteristics and generate Suno prompts",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site, no build needed'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@distube/ytdl-core": "^4.16.12"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
